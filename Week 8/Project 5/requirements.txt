# Project 5: Churn prediction for Ravenstack
# Python 3.8+
#
# Install:
#   pip install -r requirements.txt
#
# Copy .env.example to .env and add your API key before running
# any code that calls the LLM APIs.

# ------------------------------------------------------------------
# Data analysis and dashboard
# ------------------------------------------------------------------
# Used in: dashboard_compute_data.py, dashboard_build.py
pandas>=3.0
numpy>=2.0

# ------------------------------------------------------------------
# Machine learning (churn model — Part 2)
# ------------------------------------------------------------------
# Baseline model (logistic regression, preprocessing)
scikit-learn>=1.5
# Gradient boosting alternative if logistic regression underperforms
lightgbm>=4.0

# ------------------------------------------------------------------
# LLM agent stack (Parts 3 and 4)
# ------------------------------------------------------------------
langchain>=1.3
langgraph>=1.2
langchain-openai>=0.3
langchain-anthropic>=0.3

# ------------------------------------------------------------------
# API clients (use one or both, depending on which key you have)
# ------------------------------------------------------------------
openai>=1.0
anthropic>=0.40

# ------------------------------------------------------------------
# Environment and configuration
# ------------------------------------------------------------------
# Loads OPENAI_API_KEY / ANTHROPIC_API_KEY from .env
python-dotenv>=1.0

# ------------------------------------------------------------------
# Standard library (no install needed, listed for reference)
# ------------------------------------------------------------------
# json, pathlib, random — used in dashboard scripts, built into Python
